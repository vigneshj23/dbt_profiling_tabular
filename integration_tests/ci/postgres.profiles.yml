integration_tests:
  outputs:
    postgres:
        type: postgres
        host: ${POSTGRES_HOST}
        user: ${POSTGRES_USER}
        password: ${POSTGRES_PASSWORD}
        port: ${POSTGRES_PORT}
        database: ${POSTGRES_DATABASE}
        schema: ${POSTGRES_SCHEMA}
        threads: 4
  target: postgres
integration_tests:
  outputs:
    postgres:
        type: postgres
        host: ${POSTGRES_HOST}
        user: ${POSTGRES_USER}
        password: ${POSTGRES_PASSWORD}
        port: ${POSTGRES_PORT}
        threads: 4
  target: postgres
integration_tests:
  target: snowflake
  outputs:
    snowflake:
      account: ${SNOWFLAKE_ACCOUNT}
      database: ${SNOWFLAKE_DATABASE}
      password: ${SNOWFLAKE_PASSWORD}
      role: ${SNOWFLAKE_ROLE}
      schema: ${SNOWFLAKE_SCHEMA}
      threads: 4
      type: snowflake
      user: ${SNOWFLAKE_USER}
      warehouse: ${SNOWFLAKE_WAREHOUSE}

integration_tests:
  target: postgres
  outputs:
    postgres:
        type: postgres
        host: localhost
        user: postgres
        password: jman
        port: 5434
        dbname: test_profiling
        schema: profiling
        threads: 4
